specialBarStates
[
	learn
	confirm
	alert
	browse
	ingame
	none
]

barModes
{
	mainMenu     { special="learn"   top="menu"  }
	gameDetails  { special="confirm" top="logo"  }
	gameResult   { special="alert"   top="logo"  }
	learn        { special="alert"   top="title" }
	help         { special="browse"  top="title" }
	contextMenu  { special="alert"   top="title" }
	options      { special="confirm" top="title" }
}

fields
{
	contextMenu
	{
		default
		{
			items [ "options" "credits" "statistics" "done" ]
			head = "about"
		}
		
		ingame
		{
			items [ "options" "restart" "abort" "ready" ]
			head = "ingame"
		}
	}
}

help
{
	general      { total=2 }
	game_names   { total=2 }
}

dialogs
{
	confirm     = "confirm"
	confirmList = "confirmList"
	
	restart = "confirm"
	abort   = "confirm"
	credits = "credits"
	
	letters    = "confirmList"
	difficulty = "confirmList"
	length     = "confirmList"
	// choose
}
